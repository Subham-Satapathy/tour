# Database
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Admin Default Credentials
ADMIN_DEFAULT_EMAIL=admin@example.com
ADMIN_DEFAULT_PASSWORD=admin123

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here-change-in-production

# Email (SMTP) - Used for OTP verification and invoice emails
# Option 1: Gmail (Free, 500 emails/day)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-gmail@gmail.com
# SMTP_PASS=your-app-password (generate in Google Account settings)

# Option 2: Mailtrap (Testing only)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password

# Option 3: Any SMTP provider (SendGrid, Mailgun, etc.)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your-api-key

EMAIL_FROM=noreply@tour.com
EMAIL_FROM_NAME=Tour Booking

# App URL (for email links)
NEXT_PUBLIC_APP_URL=http://localhost:3000
